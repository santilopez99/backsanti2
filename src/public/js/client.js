const socket = io();

const addButton = document.getElementsByTagName('button');

addButton[0].addEventListener('click', (event) => {
    const product = {
        id: addButton[0].id
    }
    socket.emit('product', product);
});

addButton[1].addEventListener('click', (event) => {
    const product = {
        id: addButton[1].id
    }
    socket.emit('product', product);
});

addButton[2].addEventListener('click', (event) => {
    const product = {
        id: addButton[2].id
    }
    socket.emit('product', product);
});

addButton[3].addEventListener('click', (event) => {
    const product = {
        id: addButton[3].id
    }
    socket.emit('product', product);
});

addButton[4].addEventListener('click', (event) => {
    const product = {
        id: addButton[4].id
    }
    socket.emit('product', product);
});

addButton[5].addEventListener('click', (event) => {
    const product = {
        id: addButton[5].id
    }
    socket.emit('product', product);
});

addButton[6].addEventListener('click', (event) => {
    const product = {
        id: addButton[6].id
    }
    socket.emit('product', product);
});

addButton[7].addEventListener('click', (event) => {
    const product = {
        id: addButton[7].id
    }
    socket.emit('product', product);
});

addButton[8].addEventListener('click', (event) => {
    const product = {
        id: addButton[8].id
    }
    socket.emit('product', product);
});

addButton[9].addEventListener('click', (event) => {
    const product = {
        id: addButton[9].id
    }
    socket.emit('product', product);
});

addButton[10].addEventListener('click', (event) => {
    const product = {
        id: addButton[10].id
    }
    socket.emit('product', product);
});


addButton[11].addEventListener('click', (event) => {
    const product = {
        id: addButton[11].id
    }
    socket.emit('product', product);
});

addButton[12].addEventListener('click', (event) => {
    const product = {
        id: addButton[12].id
    }
    socket.emit('product', product);
});

addButton[13].addEventListener('click', (event) => {
    const product = {
        id: addButton[13].id
    }
    socket.emit('product', product);
});

addButton[14].addEventListener('click', (event) => {
    const product = {
        id: addButton[14].id
    }
    socket.emit('product', product);
});